## LangGraph Kali Runner

### Run Kali Linux tools for pentest using LangGraph

A small project that wires LangGraph workflows to run Kali tools (headless) inside containers and expose them as programmatic steps. Use this repo to prototype automated pentest tasks — only on targets you own or have permission to test.